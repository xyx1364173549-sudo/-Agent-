# ============================================
# 运行依赖
# 按模块推进逐段追加，避免一次性引入过多未使用依赖
# 安装：pip install -r requirements.txt
# ============================================

# ---------- M0 工程地基 ----------
python-dotenv>=1.0.1
