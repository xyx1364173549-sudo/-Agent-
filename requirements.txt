# ============================================
# 运行依赖
# 按模块推进逐段追加，避免一次性引入过多未使用依赖
# 安装：pip install -r requirements.txt
# ============================================

# ---------- M0 工程地基 ----------
python-dotenv>=1.0.1

# ---------- M1 LLM 接入层 ----------
# 直接用 DeepSeek 官方集成包（内部走 OpenAI 兼容协议）
langchain-deepseek>=1.1.0
