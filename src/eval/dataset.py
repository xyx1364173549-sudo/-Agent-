"""实验数据集：三组实验用到的输入，集中在这里。

三个原则：

**写死在代码里，不放外部文件**。数据集要能被版本管理、能一眼看全、
能随代码一起回滚；散在若干 CSV 里，改一个字都没人知道改过什么。

**规模刻意小**。每组实验的样本都在个位数——这是毕业论文，
不是产品评测。样本小，结论才经得起逐条复查：导师问「第三个问题
为什么是这个分」，你能立刻翻出来看。

**标注要有依据**。每个查询标注了「相关文档」，每个问题标注了
「必备要点」——没有标准答案的指标是没有意义的数字。
"""

# ==========================================================================
# 实验二：知识库资料与检索标注
# ==========================================================================

# 三篇学习资料，两两之间刻意留出「字面不重叠但语义相关」的地方，
# 这样才测得出向量检索和关键词检索的差别
MATERIALS: dict[str, str] = {
    "递归.md": """# 递归

## 什么是递归

递归是把一个大问题拆成形式相同、规模更小的问题来求解的方法。
函数在执行过程中调用它自己，直到遇到的问题小到可以直接回答为止。

## 终止条件

每一个递归都必须有出口。出口的条件一旦满足，函数就直接返回结果，
不再继续往下调用。缺少出口，程序会一层层堆下去，直到把调用栈撑满而崩溃。

出口条件还必须能让问题规模持续变小。如果每一步都在原地打转，
即使写了出口也永远走不到。

## 调用过程

函数每次调用自己，计算机都要记下「等会儿回到哪里」。这些信息堆在
一起叫做调用栈。递归返回时按相反的顺序逐层回退，把每层的结果合并起来。
""",
    "二分查找.md": """# 二分查找

## 前提

二分查找要求数据已经排好序。顺序被打乱时，折半的策略就失去了依据。

## 做法

每次比较中间位置的元素，根据比较结果把待查范围砍掉一半。
这样查找次数随数据量呈对数增长，一百万个数据最多比较二十次。

## 边界

写二分查找最容易错的地方是边界的取舍：中间位置怎么取整、
区间用闭区间还是左闭右开、循环条件是小于还是小于等于。
这三处必须彼此自洽，混用就会出现死循环或者漏查。
""",
    "Python 入门.md": """# Python 入门

## 变量与类型

变量是给数据起的名字。Python 里不需要提前声明类型，
但每个值都有自己的类型，类型决定了它支持哪些操作。

## 列表与字典

列表是一串有顺序的数据，用下标访问。字典是键值对，
按键查找而不按位置查找，适合表示「一个东西的多个属性」。

## 函数

函数把一段逻辑封装起来并起个名字，需要时按名字调用。
参数是调用时传进去的数据，返回值是函数交回来的结果。
""",
    "栈与队列.md": """# 栈与队列

## 两种存取顺序

栈只允许在一端进出，后放进去的先取出来，像叠起来的一摞盘子。
队列一端进另一端出，先放进去的先取出来，像排队买饭。

## 各自的用处

需要「回到上一步」的场景适合用栈：撤销操作、函数调用的记录、
括号是否配对。需要「按到达顺序处理」的场景适合用队列：
任务排队、消息分发、广度优先搜索。

## 常见误区

两者都不是「随便存取」的容器。少了这个限制看起来不方便，
但正因为限制，才能保证顺序不会乱。
""",
    "哈希表.md": """# 哈希表

## 核心想法

哈希表用一个函数把键换算成一个位置下标，直接去那个位置取数据，
省掉了逐个比对的过程。这就是它平均查找很快的原因。

## 冲突

不同的键换算到同一个位置，叫冲突。处理办法主要有两种：
一种是在那个位置挂一串数据逐个比对，另一种是换个位置再试。

## 取舍

哈希表用空间换时间，而且数据是没有顺序的。
需要按大小顺序遍历时，它就不合适了。
""",
    "排序算法.md": """# 排序算法

## 平方级与对数级

冒泡、插入、选择这三种做法都是两两比较，数据量翻倍时耗时会变成四倍。
归并和快速排序采用分治，把数据不断对半拆开，耗时的增长速度接近线性对数。

## 快排的隐患

快速排序在数据已经基本有序时表现最差，因为每次划分都会严重不平衡。
随机选取基准值可以大幅降低撞上这种最坏情况的概率。

## 稳定性

排序前后，值相同的元素相对次序是否保持不变，叫稳定性。
需要保留原有次序时，得挑稳定的算法。
""",
    "时间复杂度.md": """# 时间复杂度

## 它衡量什么

时间复杂度描述的是运算量随数据规模增长的趋势，不是具体秒数。
具体耗时受机器和实现影响，趋势则相对固定。

## 常见的几档

常数级与数据量无关；对数级增长很慢；线性级与数据量成正比；
线性对数级是常见高效排序的水平；平方级在数据量大时会明显吃力；
指数级基本上只适用于规模很小的问题。

## 怎么估

看循环的嵌套层数和每层循环的次数随规模怎么变化。
只保留增长最快的那一项，省略常数系数——它们不影响趋势。
""",
    "动态规划.md": """# 动态规划

## 与递归的关系

动态规划处理的仍然是「大问题拆成小问题」这一类题型，
区别在于它会**把已经算过的结果存起来**，下次需要时直接查表，
而不是重新算一遍。这一步叫记忆化，能把指数级的重复计算降到多项式级。

## 两个前提

问题的最优解要能由子问题的最优解拼出来；
子问题之间要有重叠，否则存下来也没人复用。

## 常见形态

一种是自上而下：写成递归，加一张表缓存结果。
另一种是自下而上：从最小的子问题开始填表，按依赖顺序推到最后。
两者结果一样，后一种通常省掉了递归的函数调用开销。
""",
}


# 查询与相关文档的标注。
#
# ## 为什么用「重叠度分档」而不是「字面型 / 语义型」二分
#
# 第一版想按「字面重叠高 / 低」把查询分成两类。实测发现根本分不开：
# 中文技术资料的领域词太集中，只要问题问的是同一件事，几乎必然会
# 撞上几个字（「运算量随数据量怎么涨」的重叠度高达 0.78）。
# 强行二分只会得到一个全在一类的表格。
#
# 改成分三档统计重叠度——它是连续值，信息量更大，也不会被
# 人为的阈值划分掩盖掉「低重叠查询确实更难」这个趋势。
#
# 重叠度由 ``literal_overlap`` 算出来，不靠人工判断。
RETRIEVAL_QUERIES: list[dict] = [
    # ---- 高重叠：几乎照抄了资料里的说法 ----
    {"query": "递归的终止条件是什么", "relevant": ["递归.md"]},
    {"query": "二分查找为什么要先排序", "relevant": ["二分查找.md"]},
    {"query": "Python 的字典和列表有什么区别", "relevant": ["Python 入门.md"]},
    {"query": "栈和队列的存取顺序", "relevant": ["栈与队列.md"]},
    {"query": "哈希表怎么处理冲突", "relevant": ["哈希表.md"]},
    {"query": "快速排序最坏情况是什么", "relevant": ["排序算法.md"]},
    {"query": "时间复杂度的平方级和线性对数级", "relevant": ["时间复杂度.md"]},
    {"query": "动态规划的记忆化", "relevant": ["动态规划.md"]},
    # ---- 中等重叠：换了说法，但还留着领域词 ----
    {"query": "每次只看中间那个数为什么就够了", "relevant": ["二分查找.md"]},
    {"query": "怎么用一个名字存一堆相关的东西", "relevant": ["Python 入门.md"]},
    {"query": "数据本来就差不多有序时会不会变慢", "relevant": ["排序算法.md"]},
    {"query": "算过的结果能不能留着下次直接用", "relevant": ["动态规划.md"]},
    # ---- 低重叠：用生活化的说法问同一件事 ----
    {"query": "反复问自己同一个问题直到能直接答出来", "relevant": ["递归.md"]},
    {"query": "像食堂打饭那样一个一个来", "relevant": ["栈与队列.md"]},
    {"query": "能不能不挨个翻就直接到位", "relevant": ["哈希表.md"]},
    {"query": "为什么有的整理办法省时间有的费时间", "relevant": ["排序算法.md"]},
    {"query": "数据翻一倍要多花多少时间", "relevant": ["时间复杂度.md"]},
    {"query": "程序跑着跑着突然挂掉是什么原因", "relevant": ["递归.md"]},
]


# ==========================================================================
# 实验一：分层记忆消融
# ==========================================================================

# 模拟一位学生的完整学习过程。三个实验条件用同一份数据，
# 差别只在于「给模型看多少记忆」。
STUDENT_SESSION = {
    "user_id": "exp-student",
    "session_id": "exp-session",
    # 对话轨迹：一问一答，学生的回答刻意暴露出薄弱点
    "dialogue": [
        ("user", "我想学递归，但总感觉绕不过来"),
        ("assistant", "递归要抓住两点：终止条件和问题规模缩小，我们从终止条件开始。"),
        ("user", "递归我还是不太会，尤其是不知道什么时候该停"),
        ("assistant", "可以先把出口条件写出来，再写递归那一步。"),
        ("user", "我练了 5 道题，错了 3 道，都是终止条件写错的"),
        ("assistant", "那说明补弱重点就在出口条件的判断上。"),
    ],
    # 事件流：直接写进情景记忆
    "events": [
        {"event_type": "struggled", "content": "练习递归 5 题，答错 3 题，都是终止条件写错", "importance": 0.9},
        {"event_type": "completed", "content": "完成「递归入门」的讲解", "importance": 0.6},
    ],
    # 掌握度：直接写进画像
    "mastery": {"递归的终止条件": 0.2, "递归的调用过程": 0.35, "二分查找": 0.75},
    "goal": "掌握递归",
}

# 只能靠「了解这个学生」才能答好的问题。每个问题附必备要点，
# 要点就是判分的标准答案。
PROFILE_QUESTIONS: list[dict] = [
    {
        "question": "根据你对我的了解，我接下来应该重点学什么？",
        "points": [
            "指出薄弱点在递归的终止条件",
            "提到我练习的正确率或者答错的情况",
            "给出具体可执行的学习动作",
        ],
    },
    {
        "question": "我最近的学习情况怎么样？",
        "points": [
            "提到练习的题数（5 题）",
            "提到答错的数量（3 题）",
            "指出掌握程度偏低或者还不牢固",
        ],
    },
    {
        "question": "我还需要继续学递归吗？我已经会二分查找了。",
        "points": [
            "指出递归还需要继续学",
            "给出递归的具体薄弱环节",
            "提到二分查找已经掌握",
        ],
    },
]

# 三个实验条件。名字会出现在图表里，起得能一眼看懂。
MEMORY_CONDITIONS = [
    {"key": "none", "label": "无记忆", "use_working": False, "use_episodic": False, "use_semantic": False, "use_profile": False},
    {"key": "working", "label": "仅工作记忆", "use_working": True, "use_episodic": False, "use_semantic": False, "use_profile": False},
    {"key": "full", "label": "全分层记忆", "use_working": True, "use_episodic": True, "use_semantic": True, "use_profile": True},
]


# ==========================================================================
# 实验三：动态规划 vs 静态路径
# ==========================================================================

# 知识点图。依赖关系决定「必须先学什么」。
#
# **注意这里的排列顺序是刻意打乱的**：它是「课程大纲的顺序」，
# 而大纲顺序不一定等于依赖顺序——现实中的教材就是这么排的
# （先讲个综合例子，再回头补基础）。
#
# 实验三里，静态课表按这个顺序轮转，动态规划按拓扑顺序推进，
# 差别就从这里来：排在前面的「返回值合并」依赖「调用过程」，
# 而后者还没学，练它纯属白费。
KNOWLEDGE_GRAPH: list[dict] = [
    {"topic": "递归的返回值合并", "depends_on": ["递归的调用过程"]},
    {"topic": "递归的终止条件", "depends_on": []},
    {"topic": "递归的调用过程", "depends_on": ["递归的终止条件"]},
    {"topic": "尾递归优化", "depends_on": ["递归的返回值合并"]},
    {"topic": "二分查找的边界", "depends_on": []},
    {"topic": "二分查找的变体", "depends_on": ["二分查找的边界"]},
]

# 学生这次的目标：掌握递归。二分查找那两个点不在目标里。
#
# 实验三里这个划分是关键的对比点：静态课表照大纲一路发下去，
# 不管你的目标是什么；动态规划只看目标范围内该学什么。
TARGET_TOPICS = (
    "递归的返回值合并",
    "递归的终止条件",
    "递归的调用过程",
    "尾递归优化",
)

# 模拟学生的参数。
#
# 为什么不调用真实模型：实验三要对比的是**规划算法**，不是模型能力。
# 用仿真学生有三个好处——零成本、可复现、能跑很多次做统计。
# 真实模型每次回答都不一样，跑 20 遍得到 20 个不同结果，反而说不清
# 「差别是算法带来的还是模型随机性带来的」。
SIM_STUDENT = {
    # 学生的真实掌握度：低于阈值的才真正需要练
    "true_mastery": {
        "递归的终止条件": 0.30,
        "递归的调用过程": 0.20,
        "递归的返回值合并": 0.25,
        "尾递归优化": 0.15,
        "二分查找的边界": 0.80,
        "二分查找的变体": 0.35,
    },
    # 练习一次能提升多少（掌握度增量）
    "gain_per_practice": 0.28,
    # 噪声：模拟「蒙对」和「失手」。固定随机种子时结果完全可复现
    "noise": 0.15,
    # 已经掌握到这个程度就算「不用练了」
    "target": 0.70,
}
