[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts = -v --tb=short --strict-markers
markers =
    network: 需要真实网络与 API Key 的测试（默认执行时请留意，可用 -m "not network" 跳过）
