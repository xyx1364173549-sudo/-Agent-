# ============================================
# 开发依赖（测试、覆盖率、代码检查）
# 安装：pip install -r requirements-dev.txt
# ============================================
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
