# ============================================
# 开发依赖（测试、覆盖率、代码检查）
# 安装：pip install -r requirements-dev.txt
# ============================================
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0

# 论文实验图表生成（scripts/gen_experiment_charts.py 用）。
# 论文阶段的开发依赖，不是运行依赖，所以放这里不放 requirements.txt
matplotlib>=3.10
